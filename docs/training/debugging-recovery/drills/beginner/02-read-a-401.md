# Beginner 02 — Read a 401

## Evidence pack
Browser Network:
- `GET /api/me` → **401**
- Request headers: no `Authorization`, Cookie: empty
- UI shows "Welcome, user" from `localStorage.user = {"name":"Ada"}`

## Your job
1. Failure boundary?
2. FACT vs HYPOTHESIS about why UI shows Welcome
3. Safest next experiment (one)
