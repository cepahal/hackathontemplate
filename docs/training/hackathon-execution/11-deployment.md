# 11 — Deployment

Use `checklists/production.md` and `scripts/validate/preflight.sh`.

Smoke test **the demo path** on the production URL with the demo account.
CORS, auth callbacks, and env vars fail more demos than algorithms.
