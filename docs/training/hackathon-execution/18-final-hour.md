# 18 — Final 60-Minute Protocol

**No feature coding.**

Follow `checklists/final-60.md` exactly:
freeze confirmation → git → build → tests/type/lint if fast → deploy → env → DB → auth → demo account/data → primary flow → fallback → screenshots → pitch → backup recording.
