# 09 — Demo Data

Requirements:
- Realistic names (not `test1` / `asdf`)
- Seed script idempotent
- Deterministic initial state
- Predictable AI path when `DEMO_MODE` on
- No embarrassing placeholders on high-vis screens

Own a `scripts/` or `scripts/hackathon/seed-demo.sh` when app exists.
