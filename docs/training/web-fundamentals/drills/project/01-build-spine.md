# Project 01 — Build the spine

Implement (or sketch files for) notes app spine:
- POST /api/notes (authn, owner server-set)
- GET /api/notes (filter owner)
- GET /api/notes/{id} (authz)
- React list + create form

Submit: file list + one sequence diagram of create note.
