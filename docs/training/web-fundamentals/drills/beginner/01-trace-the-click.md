# Beginner 01 — Trace the click

User clicks **Refresh** on a dashboard.

Without looking at code, list every hop from click → UI update (use the mental model).

Then mark which hop you'd inspect first if the UI stayed blank.

Label FACT vs HYPOTHESIS — you have no runtime evidence yet, so almost everything is hypothesis structure, not fact.
