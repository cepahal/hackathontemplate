# Advanced 03 — OAuth cookie trap

Callback sets `Set-Cookie; Secure; SameSite=None`. Local app is `http://localhost:3000`.
`/api/me` → 401 after "successful" login.

Explain FACT chain and fix options for local vs prod.
